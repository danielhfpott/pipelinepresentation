# Claude Code Router

Read `CONTEXT_ENTRYPOINT.md` before making any substantive project, code, presentation, or architecture decision.

Do not assume `main` is current. Verify branch/commit first.

Do not delete, reset, clean, move, rename, overwrite, force-push, or otherwise destructively normalize the workspace.

If repository evidence conflicts with chat memory or generated documentation, preserve the conflict and prefer stronger runtime/code/source evidence.

Keep this file small. The durable project context lives in the files routed from `CONTEXT_ENTRYPOINT.md`.